#!/usr/bin/env nextflow

manifest {
  mainScript = 'rrun.nf'
}

singularity {
  enabled = true
  autoMounts = true
}

process {
  executor = 'slurm'
}

params {
    // Default HPC resource levels
    // memory = 224
    // max_memory = params.memory.GB
    max_memory = 224.GB

    max_cpus = 32
}