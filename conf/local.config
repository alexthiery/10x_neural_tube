#!/usr/bin/env nextflow
/*
 * -------------------------------------------------
 *  alexthiery/10x_neural_tube docker config file
 * -------------------------------------------------
 * Specific config for running locally
 */
docker.enabled = true

params {
  max_memory = 12.GB
  max_cpus = 4
}